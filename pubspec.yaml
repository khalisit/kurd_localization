name: kurd_localization
description: "A Flutter localization package translations and Kurdish support."
version: 1.0.2
homepage: https://github.com/khalisit/kurd_localization
repository: https://github.com/khalisit/kurd_localization

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ">=0.19.0 <0.21.0"
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
